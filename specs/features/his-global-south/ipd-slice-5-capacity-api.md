# IPD slice 5 — Capacity API (placeholder)

**Project:** `his-global-south` | **Feature:** `ipd`

**Goal:** `ipd_capacity_*`, `/api/v1/ipd/capacity/live`, assign API. **Do not** use `/api/platform/beds`.

**Depends on:** slice 3.

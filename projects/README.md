# Application clones

Runnable app code: **`his-global-south/`** (must exist after `.\scripts\setup.ps1`).

## See code in Cursor

1. **Open workspace file** (recommended):  
   `File → Open Workspace from File` → `ai-orchestrator-workspace.code-workspace`

2. Or expand **`projects/his-global-south/`** in the file tree (no longer hidden by hub `.gitignore`).

## Review without running

Slice implementations are copied next to their spec, e.g.:

`specs/features/his-global-south/ipd-slice-0-scaffold/code/`

## Clone missing?

```powershell
.\scripts\setup.ps1
```

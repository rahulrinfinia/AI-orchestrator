# Per-project ADRs

One subfolder per product:

```text
docs/decisions/
├── ipd/           ← IPD decisions (0001–0004)
└── flowmd/        ← add when FlowMD registered
```

Do not share ADRs across products unless explicitly marked as workspace-wide.

Index: [README.md](README.md)

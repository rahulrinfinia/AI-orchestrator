# Documentation

| Path | Purpose |
|------|---------|
| `docs/architecture.md` | Per-project system map (create when first app is registered) |
| `docs/conventions/` | Stack coding standards for implement + review |
| `docs/features/` | Feature PRDs mirrored from `prd/` for easy discovery |
| `docs/templates/` | Copy-paste starters for PRD, slices, plans, approval |

Run discovery against cloned repos in `projects/` when onboarding a new app.
